-- @autoGeneratedPreamble@

local dot = vim.env.DOTFILES
if not dot or dot == "" then
  vim.notify("DOTFILES is not set. Please set DOTFILES to your dotfiles root (e.g. $HOME/dotfiles).",
    vim.log.levels.ERROR)
  return
end

local dot_nvim = vim.fn.expand(dot .. "/nvim")
if vim.fn.isdirectory(dot_nvim) == 0 then
  vim.notify("dotfiles nvim dir not found: " .. dot_nvim, vim.log.levels.ERROR)
  return
end

-- Make dotfiles nvim visible to runtimepath, then load its init.lua
vim.opt.runtimepath:prepend(dot_nvim)
dofile(dot_nvim .. "/init.lua")
