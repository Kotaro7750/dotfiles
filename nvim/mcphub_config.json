{
  "mcpServers": {
    "git": {
      "args": [
        "run",
        "--rm",
        "-i",
        "--mount",
        "type=bind,src=${MCPHUB_PROJECT_ROOT},dst=${MCPHUB_PROJECT_ROOT}",
        "-w",
        "${MCPHUB_PROJECT_ROOT}",
        "mcp/git",
        "--repository",
        "${MCPHUB_PROJECT_ROOT}"
      ],
      "command": "docker",
      "disabled": false
    }
  },
  "nativeMCPServers": []
}
